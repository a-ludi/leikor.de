<div id="message_window">
  <% form_tag @secure_user_request do %>
    <div class="message">
      <p>
        Sie wurden bei LEIKOR als neuer <%= @secure_user_request.user.class.human_name %>
        registriert. Bitte bestätigen Sie die Registrierung mit der Vergabe eines Passworts. Das
        Passwort muss <b>mindestens 6 Zeichen</b> lang sein und darf <b>beliebige Zeichen</b>
        enthalten.
      </p>
      <p>
        Ihr Login lautet: <b><%= @secure_user_request.user.login %></b>
      </p>
    </div>
    <% if flash[:passwords_not_matching] %>
      <div class="message error">
        Die Passwörter stimmen nicht überein.
      </div>
    <% end %>
    <div class="message">
      <p>
        <%= label_tag :password, t('secure_user_request.password') %><br />
        <%= password_field_tag :password %>
      </p>
      <p>
        <%= label_tag :confirm_password, t('secure_user_request.confirm_password') %><br />
        <%= password_field_tag :confirm_password %>
      </p>
    </div>
    <div class="response">
      <%= a_button_to(
        'Anfrage abbrechen',
        destroy_secure_user_request_path(@secure_user_request.external_id),
        :confirm => 'Die Anfrage wird endgültig abgebrochen. Sind Sie sicher?'
      ) %>
      <%= submit_tag 'Speichern!' %>
    </div>
  <% end %>
</div>
