page.replace(
  @html_id ? @html_id : @category.html_id,
  :partial => @partial,
  :locals => {:category => @category, :html_id => @html_id}
)
page << "$('#{@category.class.name.underscore}_name').focus()" if @partial == 'form'
