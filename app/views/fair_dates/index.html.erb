<div class="administration">
  <% if superuser_logged_in? %>
    <%= link_to(
      image_tag('pictogram/medium/new.png'),
      new_fair_date_path,
      :class => 'toolbutton',
      :title => 'Neuen Messetermin erstellen'
    ) %>
  <% end %>
</div>
<% unless @fair_dates.empty? %>
  <ul id="fair_dates">
    <% @fair_dates.each do |fair_date| %>
      <%= render :partial => 'fair_date', :locals => {:fair_date => fair_date} %>
    <% end %>
  </ul>
  <%=clear_float %>
<% else %>
  <h1 class="empty">
    Zur Zeit keine Messetermine.
  </h1>
<% end %>

